name: ble_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^4.0.2
  get: ^3.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
